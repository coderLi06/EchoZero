# EchoZero——项目配置

> 版本：V0.7
> 日期：2026-08-30
> 用途：项目长期事实来源；发生冲突时，以本文件最新已确认项为准。

## 1. 已冻结事项

- 重庆大学大一暑假程序设计实训，约 228 人参与；
- 目标冲击前 6；
- PC 平台、Python；
- 动作 Roguelike + 战术因果编排，弱剧情、随机奖励、Build、单局成长、轻量局外成长；
- 8 天开发口径；
- 最终只交付两个完整、高质量、有机制差异的关卡；
- Demo First、Showcase First、Vertical Slice；
- 评分优先级：创新 > 演示 > 技术 > 两关完成度 > Build > 反馈 > 市场解释 > 内容量；
- 架构遵守 KISS、YAGNI、数据驱动、逻辑与表现分离；
- 未确认核心方案前不得大规模编码。

## 2. 已确认核心方案

| 项目 | 推荐值 | 状态 |
|---|---|---|
| 正式名称 | EchoZero | 已确认 |
| 框架 | pygame-ce | 已确认 |
| Python | 当前机器 3.14.2 | 已通过 pygame-ce 2.5.8 灰盒与无窗口冒烟验证 |
| 表现 | 2D 科幻像素风、俯视角 | 已确认 |
| 战斗 | 单角色俯视实时动作；三拍规划作为有冷却的 Tactical Mode | 已确认（2026-08-30） |
| 主创新 | 三拍命令链 + 因果预演 | 已确认 |
| 辅助创新 | 协议插件 Build、规则节点争夺 | 已确认 |
| Level 1 | 校准舱：稳定规则与核心教学 | 已确认 |
| Level 2 | 逆相反应堆：周期改写规则 | 已确认 |
| Procedural Run | Seeded 随机地图、行为树敌人、三选一奖励与完整 Run 循环 | 已确认（2026-08-30） |

### Action Roguelike 定位变更（2026-08-30）

正式体验比例调整为动作战斗约 60%、策略决策约 25%、Roguelike Build 约 15%。玩家使用 WASD 实时移动，并可攻击、闪避和释放控场技能；三拍系统不删除，改为按 Q 进入的短暂 Tactical Mode，暂停实时战斗后查看同源 Enemy Intent、编辑最多三拍、Preview 并快速 Execute，随后进入冷却。原 Level 1/2 与 Stable/Reverse 保留为 Tutorial / Showcase；新增 Procedural Run 作为正式重复游玩入口，不覆盖原双关内容。

### Level 1 正式流程（Stage03 冻结）

`校准舱`固定为三个短遭遇：`序列校准`用同三招换序教学主创新；首次胜利后从 `回声协议 / 动能增幅 / 应急屏障` 三选一；`协议验证`立即让选择改变下一组三拍；`双重锁定`以突进体和校验射手的组合完成高潮检验。玩家生命和 Build 在关卡内继承，敌人状态按遭遇重建，第三战胜利后进入明确的 `LEVEL CLEAR`。

### Stage04 构筑深化（2026-08-29）

不改变上述三场 Encounter 与首次冻结三选一；在 `协议验证`胜利后增加第二次随机三选一，用于形成回声编排、动能破阵或屏障反推联动。协议总数封顶 8 个，正常 Run 使用新 Seed，测试与演示可固定 Seed；本阶段不开发 Level 2。

### Level 2 正式规则（Stage05）

`逆相反应堆`固定为三个短遭遇。战场规则在`稳定`与`逆相`之间周期切换：稳定按 1→2→3 执行，逆相按 3→2→1 执行；玩家若在回合结束时占据可见的相位锚，则锁定当前规则一回合。Level 2 增加可公开多格锁定的扫掠体，最终战使用相位守卫与现有敌人组合。Level 1 的生命、Build 和 Run Seed 继承到 Level 2；不新增协议、角色或第三关。

## 3. 推荐核心循环

New Run → Seeded 程序地图 → 实时动作战斗 → 危险时进入三拍 Tactical Mode → 随机三选一 → 下一程序 Encounter → 精英/Boss → 死亡/通关与轻量解锁 → 新 Seed。

原 Showcase 循环继续保留：开始 → 第一关战斗 → 编排/预演/执行 → 随机插件 → 第一关结算 → 第二关规则变化 → Boss 反制 → 总结算。

单个战斗回合：敌人移动并锁定公开意图 → 玩家编排 3 个命令槽 → 系统自动预演 → 玩家确认 → 3 条玩家命令依次结算 → 存活敌人按编号执行意图 → 环境与状态收尾。

### Showcase 三拍结算边界

- 不实现敌我真正同时行动；
- 三槽可为空，空槽自动视为待机；
- 首批命令仅有移动、推击、牵引、护盾；
- 玩家在执行前可无限编辑，执行后不能回溯；
- 敌人意图在规划阶段锁定，攻击固定格或方向；
- 战斗不使用命中率，随机只用于奖励、敌人组合和关卡种子；
- 推击只移动一格；被墙或实体阻挡时不连锁推动，停留并受到 1 点碰撞伤害；
- 实体死亡立即移除并取消尚未执行的攻击；危险格在进入时立即触发。

### Procedural Run 边界

- 实时移动、攻击、闪避、技能、碰撞、伤害和死亡保持小而清楚，不制作复杂连招；
- 玩家默认使用距离 1、基础伤害 2 且可击退的近战；按 `C` 切换为沿朝向扫描最多 3 格、遇墙停止、只命中首个敌人的远程攻击，伤害为当前近战伤害的一半且不击退；
- 行为树的当前 Action 同时驱动实时执行与 Enemy Intent，不维护第二套 AI；
- 程序地图用注入的 `random.Random`、连通性搜索、最小距离和失败重试生成，同 Seed 必须复现；
- Tactical Mode 调用现有唯一 `simulate_turn` 完成 Preview/Execute；表现动画只消费 LogicEvent；
- 单局控制为 3 个程序 Encounter（末场精英/Boss），不增加第三个手工关卡、角色或协议总数。
- 第三场相位守卫固定为 12 HP、普通伤害 3、十字相位爆发伤害 4；特殊攻击公开锁定中心与四个正交相邻格，半血后缩短准备冷却。

## 4. 内容预算上限

- 正式关卡：2；
- 普通敌人：3～5 种；
- Boss：1；
- 基础命令：5 种以内；
- 可选协议插件：6～8 个；
- 规则节点：3 类以内；
- 一次完整 Demo：10～14 分钟；
- 评审展示路线：3 分钟；
- 素材数量不设“约 100 张”的硬指标，以可见质量和风格统一为准。

## 5. 技术原则

- 战斗事实由纯 Python 逻辑层维护；
- 预演和真实执行使用同一模拟器；
- pygame 只处理输入、渲染、音频与动画；
- 配置必须在启动时校验并给出可读错误；
- 一局使用独立种子，随机结果可复现；
- 高频内容 JSON 配置化，但不制作通用编辑器；
- 优先 PyInstaller `onedir` 打包；
- 不引入联网、数据库或大模型运行时依赖。

## 6. 成功标准

评委能在 60 秒内理解“换顺序会改变结果”，在 2 分钟内看到 Build 改写命令规则，在 3 分钟内看到第二关规则质变；两关连续可玩；技术能够从画面对应到算法；连续 5 次三分钟演示没有阻断 Bug。

## 7. 变更规则

任何新增功能必须说明：提升哪一评分项、现场在哪里可见、实现和测试成本、若延期先砍什么。涉及战斗形式、主创新、框架、关卡职责的变更必须先更新本文件和 `INNOVATION.md`/`ARCHITECTURE.md`，再改代码。

## 8. AI 协作开发经验（2026-08-30 更新）

本节只记录开发方法，不改变以上冻结玩法和技术边界。

| 高频问题 | 本项目实际表现 | 固化做法 |
|---|---|---|
| 只改代码、不看运行画面 | 长说明溢出、Tactical 扫描过强、Build 区偏空只能在截图中发现 | 每次 UI 改动必须实际启动，覆盖六个关键状态，至少审图回修一次后再 Smoke |
| 用固定偏移掩盖窗口问题 | Windows 恢复后曾使用过期 Surface，resize 后才自愈 | 所有窗口事件统一调用幂等 `update_viewport_layout()`，布局基于实时 display Surface |
| 把内部实现直接暴露给玩家 | Behavior Tree、PreparedAction 和内部 ID 会让正式 HUD 像 Debug 工具 | 正式 HUD 使用玩家文案；F3 技术面板独立展示算法细节 |
| 为预演另写一套规则 | 容易产生 Preview 与 Execute 不一致 | 两者必须调用同一个 `simulate_turn`，并用 fingerprint 自动比较终态 |
| 把动画当成战斗逻辑 | 容易因 hit stop、击退动画改变真实坐标或伤害 | 动画只消费 `LogicEvent`；领域状态先确定，表现层只回放 |
| 未分清输入层和玩法层 | WASD 问题容易被误判为碰撞/地图/Renderer 故障 | 逐层记录 event→pressed→vector→collision→position→render；确认英文输入法链路正常后停止扩大修改 |
| 为“显得完整”添加无关技术 | 数据库、联网大模型、通用框架会增加答辩风险 | 坚持 KISS/YAGNI；只做可见、可测、能提高评分的功能 |
| 文档声明超过真实证据 | 自动 Smoke 不能等同真人盲测，配置远程也不等同已推送 | 区分代码侧完成、自动验证、人工验证和外部待确认四种状态 |

AI 在本项目中用于需求澄清、方案比较、代码辅助、测试生成、视觉审查和文档整理；运行时不依赖大模型或网络。游戏内“AI”是确定、可解释、可复现的 Behavior Tree/规则决策，适合现场答辩展示。
