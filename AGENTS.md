# AGENTS.md——Codex 项目协作规则

## 1. 项目目标

这是 8 天完成的 Python 竞赛 Demo，不是完整商业游戏。所有工作服务于：主创新清楚可玩、两个关卡完整且不同、技术可解释、现场稳定、冲击前 6。

## 2. 开工前必读

按顺序阅读：

1. `项目配置.md`：已冻结事项与待确认项；
2. `INNOVATION.md`：创新机制和边界；
3. `ARCHITECTURE.md`：模块职责和依赖方向；
4. `ROADMAP.md`、`TASKS.md`：当前阶段与任务；
5. `DEMO_PLAN.md`：可见验收路径。

若文档冲突，不擅自选择；以 `项目配置.md` 最新已确认项为准，并报告冲突。

## 3. 停止条件

负责人尚未确认核心战斗、主创新、pygame-ce 和两关定位前，只允许修改文档或制作可丢弃灰盒验证，不得生产正式内容。不得自行跨越 `ROADMAP.md` 的阶段门。

## 4. 实现规则

- KISS/YAGNI；禁止 Manager 泛滥、服务定位器和无实际消费者的抽象；
- `src/domain` 禁止导入 pygame；规则、预演和 AI 必须可在无窗口测试中运行；
- 预演与真实执行必须调用同一模拟器，禁止维护两套结算；
- 动画和音效只消费逻辑事件，禁止在动画回调中改变伤害、位置或状态；
- 技能、敌人、关卡和奖励优先配置化，但只有出现第二个真实变体后才抽象；
- 所有随机由注入的 `random.Random` 实例产生；测试和演示记录种子；
- 路径使用 `pathlib`，不写死本机绝对路径；
- 配置读取必须校验必填字段、ID 唯一性和引用存在性；
- 捕获异常时保留上下文，不使用空 `except`；
- 不引入网络、数据库、遥测或大模型运行时依赖。

## 5. 代码与数据约定

- Python 代码使用类型标注；类名 `PascalCase`，函数/变量/文件 `snake_case`，常量 `UPPER_SNAKE_CASE`；
- ID 使用稳定英文蛇形命名，如 `echo_protocol`；显示文本放数据文件；
- 领域数据优先 `dataclass` 和 `Enum`，不要用无结构大字典跨模块传递；
- 命令执行返回显式事件列表；不得直接调用渲染器；
- JSON 只存数据，不在配置里嵌入 Python 表达式；
- 单文件超过约 350 行或包含两类职责时优先拆分，但不为行数机械拆分。

## 6. 测试门槛

每个规则改动至少覆盖正常路径和一个边界条件。核心必测：

- 预演状态等于相同命令真实执行后的状态；
- 推动、碰墙、死亡后续命令取消；
- 状态叠加/到期与节点切换；
- AI 在固定状态和种子下输出固定意图；
- 配置缺失、重复 ID、坏引用能快速失败；
- 保存后读取保留 Build、关卡和 RNG 必要状态。

合并前至少运行纯逻辑测试、启动冒烟测试和当前关卡最短通路。D7 起每次发布均测试 PyInstaller onedir 包。

## 7. 变更控制

开始功能前在 `TASKS.md` 建项，说明它提高的评分维度和可见验收。完成后同步状态、测试和相关文档。若工作量大但评委难以看见，默认不做。

禁止未经负责人同意扩大：关卡数、角色数、敌人上限、插件上限、剧情和局外成长范围。

## 8. 素材与版权

只使用自制、明确可商用/可署名授权或课程允许的素材；记录来源和许可证。占位素材与正式素材分开。不要把互联网图片直接放入最终包。

## 9. Git 建议

进入阶段 01 时初始化仓库。小步提交，格式建议 `type(scope): message`。提交前检查大文件、缓存、构建产物和本机路径。禁止重写他人未合并历史或覆盖未知修改。

## 10. 完成定义

“完成”意味着功能可从实际游戏路径触发、反馈可见、自动测试通过、没有新增阻断 Bug、文档和任务状态同步；只有代码存在不算完成。
