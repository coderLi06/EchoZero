# EchoZero 答辩前预评分报告

> 评估日期：2026-09-02；评估依据：工作区 `D:\daerxia\程序设计实训\project` 全部文件、代码、测试与文档。所有结论均引用具体文件为证据，无法从文件中确认的内容标记"待现场验证"。

> 2026-09-02 后续更新：原报告中的“真人盲测缺失”与“onedir 发布包未完成”已解决。已完成 3 名新玩家盲测（通关 2/3，无阻断问题），并完成 PyInstaller onedir 发布包及 Action/Showcase 两条发布版 Smoke；结构与外部字体闭环后的全量测试增至 131 项并全部通过。

---

## 一、当前预计总分及分数区间

**预计总分：78～84 分（满分 100）**

分数区间说明：
- 保守分：76 分（外部待验证项全部不通过、Git 未推送被扣分）
- 正常分：82 分（外部待验证项部分通过、Git 已推送）
- 理想分：86 分（真人盲测完成且反馈良好、发布包验证通过、演示零失误）

---

## 二、项目答辩分项评分

### 功能：8.5 / 10

**得分依据：**

1. **核心创新已完整落地**：`src/domain/simulation.py` 实现了 `simulate_turn` 作为唯一模拟器，`preview_turn` 和 `execute_turn` 均调用同一函数（第 44-49 行），确保预演与执行同源。PRD FR-05、FR-06 已满足。

2. **双模式完整**：`main.py` 同时支持 Action Roguelike（正式入口）和双关卡 Showcase（`--showcase`），`ActionApp` 和 `Stage03App` 两条独立路径均可运行。PRD FR-01～FR-10 全部有对应代码。

3. **程序生成与 BFS 验证**：`src/domain/procedural.py` 实现了 Seeded 房间/走廊生成、BFS 连通性验证、最小距离校验和失败重试（`validate_encounter` 函数第 214-228 行）。

4. **行为树 AI**：`src/domain/behavior_tree.py` 实现了 Selector/Sequence/Condition/Action 四类基础节点和三种敌人行为树（第 98-120 行），PreparedAction 同时作为实时执行和 Enemy Intent。

5. **8 个协议插件 + 5 种属性/技能奖励**：`data/plugins/protocols.json` 配置了 8 个协议，含 requirements/conflicts/max_stack 过滤；`src/domain/action_run.py` 第 52-58 行定义了 5 种升级奖励。

6. **125 项自动化测试全部通过**：实际运行 `pytest` 确认 125 passed（`功能测试与初验报告.md` 第 35 行记录）。覆盖预演一致性、AI 固定输出、程序地图复现、Tactical 同源、Reward 三选一、教学不污染 Run 等。

7. **Smoke 测试通过**：`main.py --smoke-test` 和 `--showcase-smoke-test` 均返回退出码 0（已实际验证）。

**扣分点：**
- 真人外部盲测已完成，记录见 `docs/blind-tests/`
- PyInstaller onedir 发布包已完成并通过两条发布版 Smoke
- 中文输入法下 WASD 无响应，需现场切换英文输入法（`ARCHITECTURE.md` 第 262 行）

### 界面：8 / 10

**得分依据：**

1. **完整且已拆分的 UI 体系**：Action Renderer 已按协调/基础绘制、世界与 HUD、教学、覆盖层和共享常量拆为 5 个模块；入口 228 行、最大子模块 345 行，覆盖菜单、Action HUD、Tactical、Reward、Build、Boss、结果页和 9 步教学。

2. **多分辨率响应式**：1280×800 逻辑画布在 1600×900 输出 1440×900、1920×1080 输出 1728×1080（`DEMO_PLAN.md` 第 168 行），自动测试鼠标坐标反算通过。

3. **视觉 QA 证据充分**：`artifacts/ui_polish_final/` 22 张截图 + `artifacts/ui_clarity_final/` 12 张截图，覆盖六种核心状态和三种分辨率，经过两轮人工审图回修。

4. **中文 HUD**：正式右栏以中文呈现核心/护盾、冷却、敌方意图、战术三拍和操作说明（`ARCHITECTURE.md` 第 249 行），内部 ID 不暴露给玩家。

5. **原创美术与音频**：`src/presentation/action_art.py` 代码原生绘制五种非同形单位轮廓；`src/presentation/audio.py` 合成 20 个音效和三段原创 BGM。素材来源记录完整（`ASSET_CREDITS.md`）。

6. **Figma 设计规范**：`docs/FIGMA_ASEPRITE_UPGRADE.md` 记录了 Figma 视觉规范，五个核心画板完成。

7. **Aseprite 兼容动画**：`tools/build_aseprite_assets.py` 确定性生成 PNG 图集和 JSON 元数据，`src/presentation/action_assets.py` 接入正式 Renderer。

**扣分点：**
- Figma Motion 元数据于 2026-09-02 再次回读仍被 Starter MCP call limit 阻断；原始结果和收尾步骤已归档到 `docs/FIGMA_MOTION_AUDIT.md`，这是当前唯一未闭环项
- 已接入 SIL OFL 1.1 的 Orbitron 外部开源字体强化英文品牌标题，并保留完整许可证、来源和中文回退；角色与 UI 仍以自制 + Meowa 保持统一
- 9 月 1 日 Meowa 素材、`action_assets.py`、构建工具与 QA 证据已在提交 `ff1ce3e` 纳入 GitHub

### 技术创新：8.5 / 10

**得分依据：**

1. **主创新——三拍命令链 + 因果预演**：`INNOVATION.md` 详细记录了创新机制，`src/domain/simulation.py` 实现了同源 preview/execute。创新不是"多做一个随机技能系统"，而是让每个 Build 都能改写三拍命令（`INNOVATION.md` 第 64 行答辩解释）。

2. **辅助创新 A——协议插件 Build**：8 个协议以"条件 + 效果节点"修改命令规则（回声重放、动能增伤、碰撞过载、牵引断锁等），至少 70% 改变行为而非纯数值。三条 Build 路线（回声编排、动能破阵、屏障反推）均有 6/5/8 回合通关记录（`DEMO_PLAN.md` 第 117-122 行）。

3. **辅助创新 B——规则节点争夺**：Level 2 实现稳定/逆相周期切换（3→2→1 执行），相位锚允许空间站位锁定规则（`src/domain/simulation.py` 第 290-322 行）。

4. **DDD 架构**：`src/domain/` 无 pygame 依赖，`src/presentation/` 只消费 LogicEvent，`src/infrastructure/` 只负责配置和存档。依赖方向清晰（`ARCHITECTURE.md` 第 12-24 行）。

5. **确定性设计**：所有随机由注入的 `random.Random` 实例产生，`state_fingerprint` 可比较终态一致性，预演不消费全局随机。

6. **可解释 AI**：行为树输出唯一 PreparedAction，既是实时执行依据也是公开 Enemy Intent，F3 技术面板展示 Selector/Sequence/Condition（`AGENTS.md` 第 53 行）。

7. **代码规模**：src 目录 8414 行，tests 目录 2269 行，测试/代码比约 27%。

8. **Git 提交历史清晰**：11 个提交，从 `chore: initialize` 到 `feat: finalize`，阶段分明。

**扣分点：**
- Action Run 与 Showcase 分别承担正式程序化 Run 和两关机制答辩路径；二者共享 Command / CombatState / LogicEvent / Simulator，退役的 Stage02 pygame UI 已删除
- `action_renderer.py` 已由 1032 行拆为入口 228 行和 4 个职责模块，最大文件 345 行，满足 AGENTS.md 拆分约束
- 难度系数评估见下

### AI 协作与代码理解：16 / 20

**得分依据：**

1. **AI 协作规则文档化**：`AGENTS.md` 是完整的 Codex 项目协作规则，覆盖目标、开工前必读、停止条件、实现规则、代码约定、测试门槛、变更控制、素材版权和完成定义。`项目配置.md` 第 8 节"AI 协作开发经验"记录了 8 个高频问题及固化做法。

2. **协作过程可追溯**：`每日工作日志.md` 按日记录了从 08-27 到 08-31 的工作内容、决策、交付证据和外部事项。Git 提交历史与日志对应。

3. **AI 使用边界清晰**：PRD 第 8 节明确"不接入在线大模型"，游戏 AI 采用可解释 Behavior Tree，风险更低、技术更可信（`需求PRD.md` 第 95 行）。

4. **文档体系完整**：PRD、ARCHITECTURE、INNOVATION、ROADMAP、TASKS、DEMO_PLAN、竞品分析、功能测试报告、课程验收报告、项目配置、每日日志、素材版权清单共 12 份核心文档。

5. **任务板逐项验收**：`TASKS.md` 有 80+ 个 `[x]` 已完成项，每个都标注评分维度和验收标准。

6. **AI 协作经验总结有深度**：`项目配置.md` 第 8 节的表格不是泛泛而谈，而是针对本项目实际遇到的问题（如"只改代码不看运行画面"导致长说明溢出、"用固定偏移掩盖窗口问题"导致 Windows 恢复后过期 Surface）。

**扣分点：**
- **Git 素材风险已解决**：9 月 1 日的 Meowa、Aseprite 图集、`action_assets.py`、工具与验收证据已由 `ff1ce3e` 提交并推送
- **无法从文件确认答辩者对代码的口头理解深度**：待现场验证
- **AI 协作记录集中在文档层面**：没有对话日志或 prompt 记录（但 AGENTS.md 和日志已足够替代）
- **知识补强考试提交无法由仓库验证**（`课程任务验收报告.md` 第 21 行标注"外部待确认"）

### 项目难度系数：1.15

**依据：**
- Python + pygame-ce 从零开发，非使用游戏引擎的高级 API
- DDD 架构、确定性模拟器、同源 preview/execute、行为树 AI、程序地图生成 + BFS 验证——这些都是从底层实现的算法
- 8 天内完成 8414 行源码 + 2269 行测试 + 12 份文档 + 125 项测试
- 但项目规模仍是 Demo 级别（2 关、3-5 敌人、8 协议），不涉及网络/数据库/多人

---

## 三、每项得分依据汇总

| 评分项 | 得分 | 满分 | 关键证据 |
|---|---|---|---|
| 功能 | 8.5 | 10 | `simulation.py` 同源模拟器、`procedural.py` BFS 验证、`behavior_tree.py` 三种敌人树、125 项测试通过、两条 Smoke 通过 |
| 界面 | 8.5 | 10 | 拆分后的完整 UI、34 张最终截图、三分辨率响应式、中文 HUD、Figma 规范、Meowa 与 OFL Orbitron 外部资产 |
| 技术创新 | 8.5 | 10 | 三拍因果预演 + 协议改写命令 + 逆相规则节点 + DDD 架构 + 确定性设计 + 可解释 AI |
| AI 协作与代码理解 | 16.0 | 20 | `AGENTS.md` 协作规则、`项目配置.md` 第 8 节经验总结、`每日工作日志.md` 过程追溯、12 份文档体系、80+ 验收项 |
| 难度系数 | 1.15 | — | 从零实现模拟器/AI/程序生成，非引擎高级 API |

**加权总分计算：(8.5 + 8.0 + 8.5 + 16.0) × 1.15 = 41.0 × 1.15 ≈ 47.15（此为答辩部分）**

**若按 100 分制换算（含文档和演示分）：预计 78～84 分**

---

## 四、缺少或无法验证的材料

| 项目 | 状态 | 说明 |
|---|---|---|
| 真人外部盲测记录 | **已完成** | 3 名新玩家，通关 2/3，平均 11.3 分钟，观察者介入 0/3 |
| PyInstaller onedir 发布包 | **已完成** | PyInstaller 6.22.2 生成 `dist/EchoZero`，Action/Showcase Smoke 退出码均为 0 |
| 5 次连续 3 分钟演示演练 | **待现场验证** | 无演练记录 |
| 发布机 Win+D/最大化恢复 | **待现场验证** | 需要在答辩机上实际执行 |
| 知识补强考试提交 | **外部待确认** | 需要课程平台记录 |
| 开班签到 | **外部待确认** | 属于线下行为 |
| Git 最新代码推送 | **已完成基础素材提交** | `ff1ce3e` 已包含 Meowa、Aseprite、action_assets、工具和 QA；本轮结构优化完成验证后继续提交 |
| 答辩者口头理解深度 | **待现场验证** | 无法从文件确认 |
| 备份录屏 | **待现场验证** | 工作区中无录屏文件 |
| 现场音箱主观混音 | **待现场验证** | 程序化 BGM 仅在 dummy 音频测试通过 |

---

## 五、最可能被老师扣分的 5 个问题

### 1. Git 未提交/未推送最新代码（风险已解决）

提交 `ff1ce3e` 已把 `src/presentation/action_assets.py`、`assets/`、`tools/`、Figma/Aseprite 文档和 QA 证据推送到 GitHub；原风险不再成立。

### 2. 真人盲测已完成（风险已解决）

3 名新玩家均在无观察者介入的情况下完成测试：Preview / Reverse 理解 3/3，Phase Anchor 明确理解 2/3，2 人通关，未通关者也独立进入 Boss Encounter。原始记录和诚实的缺失项标注位于 `docs/blind-tests/`。

### 3. 中文输入法下 WASD 无响应（中风险）

`ARCHITECTURE.md` 第 262 行明确记录"中文输入法状态可能不向 pygame 提供可用游戏按键事件"。现场如果忘记切换英文输入法或答辩机输入法异常，演示会卡住。虽然有文档说明，但这是一个现场运行风险。

### 4. PyInstaller 发布包已完成（风险已解决）

PyInstaller 6.22.2 onedir 已生成 `dist/EchoZero`和 Windows x64 ZIP，从发布包运行 Action Run 与 Showcase 两条 Smoke 均返回 0。答辩机仍需执行一次人工窗口恢复复核。

### 5. Action Run 与 Showcase 双系统并存（低风险）

`ActionApp` 是正式程序 Run，`Stage03App` 是两关机制 Showcase；两者不是重复结算系统，共享领域 Command、状态、LogicEvent 与同源 Simulator。`stage02_app.py` 的 319 行退役 pygame UI 已删除，只保留约 55 行历史回归夹具。

---

## 六、按提分收益排序的改进清单

| 优先级 | 改进项 | 预计提分 | 工作量 | 说明 |
|---|---|---|---|---|
| **P0** | 提交并推送 Git 最新代码 | +2～3 分 | 10 分钟 | `git add -A && git commit && git push`，让远程仓库反映完整工作 |
| **DONE** | 完成 PyInstaller onedir 打包 | +1～2 分 | 已完成 | 发布包两条 Smoke 已验证 |
| **DONE** | 执行真人盲测并记录 | +1～2 分 | 已完成 | 3 份原始记录与 1 份汇总已归档 |
| **P1** | 答辩机 Win+D/最大化恢复验证 | +0.5 分 | 5 分钟 | 在答辩机上执行窗口恢复序列 |
| **P1** | 5 次连续 3 分钟演示演练 | +1 分 | 30 分钟 | 按 `DEMO_PLAN.md` 主路线练习，记录成功率 |
| **P2** | 录制备份演示视频 | +0.5 分 | 10 分钟 | 作为现场失败时的应急预案 |
| **DONE** | 精简 `action_renderer.py` | +0.5 分 | 已完成 | 入口 228 行，最大职责模块 345 行 |
| **DONE** | 清理 `stage02_app.py` | +0.5 分 | 已完成 | 退役 pygame UI 已删除，只保留无窗口回归夹具 |

---

## 七、如果今天答辩的三档分数

| 情景 | 分数 | 条件 |
|---|---|---|
| **保守分** | 78 分 | 演示出现输入法问题 + 答辩紧张无法解释代码；盲测和发布包不再扣分 |
| **正常分** | 84 分 | Git 已推送 + 演示顺利（英文输入法）+ 能解释核心算法 + 文档、发布包和真人盲测证据完整 |
| **理想分** | 86 分 | Git 已推送 + 演示零失误 + 答辩对答如流（能解释 simulate_turn 同源、BFS 验证、行为树节点）+ 完成发布包 + 有 1 次盲测记录 + Figma/Meowa 素材加分 |

---

## 八、综合评价

EchoZero 是一个**完成度相当高、架构清晰、文档规范**的程序设计实训项目。核心创新（三拍因果预演 + 协议改写命令）不是口号，而是真实落地在 `src/domain/simulation.py` 中，并有 125 项自动化测试验证。DDD 架构、确定性设计、同源 preview/execute、可解释行为树 AI 都是实打实的技术亮点。

原报告指出的 PyInstaller 发布包和真人盲测缺失已解决。当前剩余过程交付重点是推送最新 Git 状态、完成答辩机恢复/音箱复核与 5 次连续 3 分钟人工演练。

**建议答辩重点讲解路线：**
1. 用 `--smoke-test` 证明完整 Run 可自动走通
2. 用 F3 技术面板展示 Behavior Tree 节点
3. 在 Showcase 模式演示"换序改变结局"的强对比
4. 解释 `simulate_turn` 如何被 `preview_turn` 和 `execute_turn` 共同调用
5. 展示 `procedural.py` 的 BFS 验证逻辑
